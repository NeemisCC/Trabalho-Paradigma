import React from 'react'

const MenuSectionTitle = ({ text }: { text: string }) => {
    return (
        <h4 className="mb-4 text-sm leading-none font-bold">{text}</h4>
    )
}

export default MenuSectionTitle